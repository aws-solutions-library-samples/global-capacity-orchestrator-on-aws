# CDK
*.swp
*.tmp
~$*
cdk.out/
cdk-diff.out
.cdk.staging/
cdk.context.json
# Persistent Windows sidecar for serializing atomic cdk.json mutations.
.gco-config.lock

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.vscode/
.idea/
.kiro/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Test coverage
.coverage
# Parallel-run shards (coverage combine inputs). Untracked shards would
# otherwise fail the recorders' verify_recording_git_state, which inspects
# --untracked-files=all, after any local `pytest -n` run.
.coverage.*
coverage.json
coverage.xml
.pytest_cache/
htmlcov/
# bashcov output (the shell coverage report), and the local Bundler install
# `bundle install --path vendor/bundle` leaves behind — both would otherwise
# show up as untracked and trip verify_recording_git_state the same way.
coverage/
.bundle/
vendor/bundle/

# Kubernetes
kubeconfig*
*.kubeconfig

# AWS
.aws/
aws-exports.js

# Temporary files
tmp/
temp/
.live-release-validation/
.example-job-validation/

# Other
test_plan.md
BUG_FIXES_SUMMARY.md
DEPLOYMENT_PLAN.md
GPU_NODEPOOL_IMPLEMENTATION.md
lambda/kubectl-applier-simple-build/
lambda/helm-installer-build/
lambda/inference-streaming-proxy-build/
lambda/.*-build.lock
lambda/.*-build.staging-*
lambda/.*-build.backup-*
/node_modules/
lambda/inference-streaming-proxy/node_modules/
.hypothesis/

# Local-only security documents (never commit)
SECURITY_RESPONSE.md
THREAT_MODEL.md

# Locally generated video conversions of the committed demo GIFs
# (see demo/gif_to_mp4.sh) — the GIFs are the tracked source of truth.
demo/*.mp4
# MkDocs build output for the orientation wiki (mkdocs.yml) — built in CI
# (lint:mkdocs:strict, pages.yml), never committed.
site/
