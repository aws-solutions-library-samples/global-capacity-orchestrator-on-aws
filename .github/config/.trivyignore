# =============================================================================
# .trivyignore — Temporary Trivy suppressions
# =============================================================================
#
# Rules:
#   1. Every entry MUST have an expiration date (exp:YYYY-MM-DD).
#   2. Every entry MUST link to an upstream tracking issue/advisory.
#   3. When an entry expires, re-evaluate: remove it if the CVE is fixed,
#      or extend the date with a new justification if the fix still isn't
#      available. Never extend without re-checking upstream.
#   4. Keep the file short. A growing ignorefile is a smell.
#   5. Verify against the pinned CI Trivy (the `version` default in
#      .github/actions/install-trivy/action.yml) and its DB source —
#      local `brew install trivy` uses
#      the ghcr.io DB which can classify severities differently from the
#      CI's mirror.gcr.io/aquasec/trivy-db:2.
#
# Format:
#   CVE-XXXX-XXXXX exp:YYYY-MM-DD
# =============================================================================

# CVE-2026-56854 — golang.org/x/crypto/ssh authentication bypass caused by
# unenforced source-address restrictions, fixed in x/crypto v0.55.0. The
# latest stable Buildx v0.36.1 and Helm v4.2.4 binaries both embed v0.54.0.
# Official release candidates contain the fix, but this release deliberately
# consumes stable tooling only; no fixed stable wrapper release exists as of
# 2026-09-01. Neither binary exposes an SSH server: Buildx is contributor/CI
# build tooling and Helm is a deploy-time chart client, so the vulnerable
# server-side authentication path is not reachable. This new suppression has
# a 30-day review window and can be removed only after stable Buildx and Helm
# releases both embed x/crypto >= v0.55.0.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-56854
CVE-2026-56854 exp:2026-10-01

# CVE-2026-17106 (GHSA-hfg8-hc9c-6c3h) — github.com/moby/go-archive v0.2.1: a
# crafted tar archive can write outside the extraction directory during
# untar. Statically linked into the docker-buildx CLI plugin that
# Dockerfile.dev installs (ARG BUILDX_VERSION). Surfaced by
# security:trivy:container-scan on the GCO dev image — the only scanned image
# that carries the Buildx plugin. Fixed in moby/go-archive 0.3.0.
#
# Not introduced by any change of ours: the GHSA was published
# 2026-08-18T21:17Z and reached the Trivy DB in the following hours. The
# Security workflow was green on main at eeeab67 (2026-08-18T20:50Z), 27
# minutes before publication, so this appeared on an unchanged buildx binary.
#
# Not clearable by a stable pin bump: re-checked 2026-09-01 — v0.36.1 is
# the latest stable Buildx release and requires moby/go-archive v0.2.1. The
# available v0.37.0 release candidate still uses v0.2.0, so even accepting a
# prerelease would not clear this finding; no upstream branch uses >= v0.3.0.
#
# Low runtime risk: the flagged path is tar extraction. buildx is a
# build-time CLI plugin that extracts contexts and cache artifacts authored by
# the contributor or CI invoking it, plus the version-pinned images it pulls —
# not attacker-supplied archives. The dev image is a contributor/CI tool that
# is never deployed to a cluster.
#
# Clears when a buildx release ships requiring moby/go-archive >= 0.3.0: bump
# BUILDX_VERSION in Dockerfile.dev (the monthly deps-scan tracks it) and drop
# this entry. Advisory: https://avd.aquasec.com/nvd/cve-2026-17106
CVE-2026-17106 exp:2026-11-14

# CVE-2026-14257 (GHSA-mh99-v99m-4gvg) — brace-expansion denial of service via
# unbounded expansion length, causing an out-of-memory process crash. Present in
# the copy npm vendors inside its own node_modules
# (usr/lib/node_modules/npm/node_modules/brace-expansion) in the dev image, so
# it is surfaced only by security:trivy:container-scan on Dockerfile.dev. Fixed
# upstream in brace-expansion 5.0.8.
#
# Not introduced by any change of ours: the advisory was published 2026-07-24
# and reached the Trivy DB after the last green main run, so it appeared on an
# unchanged dev image.
#
# Not clearable by a pin bump: re-checked 2026-09-01 against npm 12.0.2 (the
# release this ARG now pins) — it still bundles brace-expansion 5.0.7,
# verified in the installed tree
# (node_modules/npm/node_modules/brace-expansion/package.json). The bundled
# copy lives inside npm's published tarball, where neither `npm audit fix`
# nor an `overrides` entry can reach it.
#
# Low runtime risk: the expansion path is reached through brace globs in npm's
# own CLI argument handling, driven by the contributor or CI invocation rather
# than untrusted remote input. The dev image is a contributor/CI tool and is
# never deployed to a cluster.
#
# Clears when npm ships a release that re-vendors brace-expansion >= 5.0.8:
# bump ARG NPM_VERSION in Dockerfile.dev (plus the packageManager pins the
# consistency check holds in lockstep) and drop this entry.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-14257
CVE-2026-14257 exp:2026-10-22

# CVE-2026-69152 (GHSA-rgw5-rvv9-x895) — brace-expansion denial of service via
# unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation
# suppressed above. Published 2026-08-01, fixed upstream in brace-expansion
# 5.0.9. Same vendored copy as CVE-2026-14257: npm ships brace-expansion 5.0.7
# inside its own node_modules in the dev image, surfaced by
# security:trivy:container-scan on Dockerfile.dev. The separate root graph is
# now clear: aws-cdk-lib 2.267.0 bundles brace-expansion 5.0.9, so the lockfile
# refresh removes the matching .npm-audit-ignore entry.
#
# This image finding is not clearable by a pin bump: re-checked 2026-09-01 —
# npm 12.0.2 is still the latest npm release and still bundles 5.0.7 in its
# immutable published tarball, beyond the reach of `npm audit fix` or an
# `overrides` entry.
#
# Low runtime risk: same exposure profile as CVE-2026-14257 above — the
# expansion path is driven by contributor/CI-authored brace globs, not
# untrusted remote input, and the dev image is never deployed to a cluster.
#
# Clears when npm ships a release that re-vendors brace-expansion >= 5.0.9:
# bump ARG NPM_VERSION in Dockerfile.dev (plus the packageManager pins the
# consistency check holds in lockstep) and drop this entry.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-69152
CVE-2026-69152 exp:2026-10-22

# CVE-2026-69192 (GHSA-mwp4-54f8-5fhr) — ip-address parses leading-zero IPv4
# octets as decimal while OS resolvers treat them as octal, enabling
# SSRF/trust-boundary bypass in code that validates an address string and
# then hands it to a resolver. Fixed upstream in ip-address 10.3.1. Present
# only as ip-address 10.2.0 vendored inside npm's own node_modules
# (socks-proxy-agent -> socks -> ip-address) in the dev image, surfaced by
# security:trivy:container-scan on Dockerfile.dev.
#
# Not clearable by a pin bump: checked 2026-09-01 — npm 12.0.2 (ARG
# NPM_VERSION in Dockerfile.dev) is the latest npm release on any dist-tag
# and still bundles ip-address 10.2.0 inside its published tarball, out of
# reach of `npm audit fix` and `overrides`.
#
# Low runtime risk: the flagged path only runs when npm connects through a
# SOCKS proxy (npm's `proxy` config), and the address it parses is the
# operator-configured proxy host — not untrusted remote input. The dev image
# is a contributor/CI tool that is never deployed to a cluster.
#
# Clears when npm ships a release that re-vendors ip-address >= 10.3.1: bump
# ARG NPM_VERSION in Dockerfile.dev (plus the packageManager pins the
# consistency check holds in lockstep) and drop this entry.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-69192
CVE-2026-69192 exp:2026-10-22

# CVE-2026-73566 — node-tar denial of service: a crafted GNU L / PAX x
# long-path header with thousands of slash-separated segments drives the
# filesFilter mapHas helper into unbounded path.dirname() recursion, and the
# resulting uncatchable RangeError terminates async/stream consumers of
# tar.t()/tar.x() called with a member-selection list. Fixed upstream in tar
# 7.5.21. Present only as tar 7.5.19 vendored inside npm's own node_modules
# in the dev image, surfaced by security:trivy:container-scan on
# Dockerfile.dev. Neither repository-owned npm graph (root or
# lambda/inference-streaming-proxy) depends on tar at all.
#
# Not clearable by a pin bump: checked 2026-09-01 — npm 12.0.2 (ARG
# NPM_VERSION in Dockerfile.dev) is the latest npm release on any dist-tag
# and declares tar ^7.5.19, shipping 7.5.19 inside its published tarball,
# out of reach of `npm audit fix` and `overrides`. Vendored-tar refresh is
# tracked upstream in https://github.com/npm/cli/issues/8917. The
# DNF_SECURITY_EPOCH knob only refreshes RPM packages and cannot touch
# npm's bundled node_modules.
#
# Low runtime risk: the vulnerable path requires npm itself to list/extract
# a hostile tarball with a selection filter; in the dev image npm handles
# registry tarballs during contributor/CI installs, impact is limited to
# process termination (DoS, no code execution), and the dev image is never
# deployed to a cluster.
#
# Clears when npm ships a release that re-vendors tar >= 7.5.21: bump
# ARG NPM_VERSION in Dockerfile.dev (plus the packageManager pins the
# consistency check holds in lockstep) and drop this entry.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-73566
CVE-2026-73566 exp:2026-11-14

# CVE-2026-50163 (GHSA-77gc-fj98-665h) — oras.land/oras-go/v2 v2.6.1
# information disclosure / arbitrary file access via crafted tarball
# hardlinks, statically linked into the pinned Helm binary
# (helm-v4.2.4-linux-amd64, lambda/helm-installer/Dockerfile). Surfaced by
# security:trivy:container-scan on the helm-installer Lambda image — the
# only scanned image that carries helm. Fixed in oras-go v2.6.2.
#
# Not introduced by any change of ours: the advisory reached the Trivy DB
# after the last green main run and appeared on an unchanged helm binary.
#
# Not clearable by a stable pin bump: re-checked 2026-09-01 — v4.2.4 is
# the latest stable Helm release and its binary still reports oras-go v2.6.1.
# v4.3.0-rc.1 contains v2.6.2, but this release deliberately excludes
# prerelease tooling; remove this suppression when a stable Helm build carries
# the fix.
#
# Low runtime risk: the flagged path is OCI-registry chart *pull*
# extraction. The helm-installer Lambda pulls only the version-pinned charts
# in charts.yaml from operator-configured registries during stack deploys — not untrusted, attacker-supplied artifacts — and the Lambda
# runs only at deploy time, not as a long-lived network service.
#
# Clears when a stable Helm release ships with oras-go >= v2.6.2: bump the
# authenticated Helm URL and SHA256 trust anchor in
# lambda/helm-installer/Dockerfile (workflows derive both pins from there),
# update the expected pins in tests/test_supply_chain_integrity.py, then drop
# this entry.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-50163
CVE-2026-50163 exp:2026-10-22

# CVE-2025-47273 (setuptools 70.3.0, fixed 78.1.1) and GHSA-6v7p-g79w-8964
# (msgpack 1.1.2, fixed 1.2.1) — both live only inside the AWS CLI v2
# vendored bundle (usr/local/aws-cli/v2/2.36.26/dist/) in the dev image,
# not in our resolvable dependency tree (msgpack appears nowhere in
# requirements-lock.txt, and site-packages setuptools is current).
#
# Re-confirmed 2026-09-01 by a pinned-Trivy 0.74.0 scan of a full dev-image
# trial build with latest AWS CLI 2.36.36: both findings remain against the
# vendored bundle. Scanning only the release zip is insufficient; the built
# image is authoritative per rule 5 above.
#
# Not clearable by a pin bump: 2.36.36 is the newest AWS CLI v2 release and
# still vendors the affected versions. Clears when a newer AWS CLI release
# rebuilds its bundle with patched deps — bump AWSCLI_VERSION in
# Dockerfile.dev and re-check.
#
# Low runtime risk: the dev image is a contributor/CI tool that is never
# deployed; the flagged code paths run only inside `aws` CLI invocations
# against operator-supplied input.
#
# Advisories: https://nvd.nist.gov/vuln/detail/CVE-2025-47273
#             https://github.com/advisories/GHSA-6v7p-g79w-8964
CVE-2025-47273 exp:2026-11-14
GHSA-6v7p-g79w-8964 exp:2026-11-14

# -----------------------------------------------------------------------------
# CVE-2026-33818 (encoding/asn1 DoS), CVE-2026-56853 (net/http unencrypted
# HTTP/2), CVE-2026-56858 (html/template XSS), CVE-2026-56859 (encoding/xml
# DoS), CVE-2026-56860 (net/url DoS), CVE-2026-56862 (crypto/tls DoS),
# CVE-2026-39821 (x/net punycode flaw with a matching net/idna stdlib copy)
# and CVE-2026-46600 (dns/dnsmessage panic on crafted SVCB/HTTPS records,
# likewise present in the stdlib resolver) — all eight are Go STDLIB flaws
# in binaries built with go1.26.5, fixed in Go 1.25.13 / 1.26.6 /
# 1.27.0-rc.3. The last two formerly sat in this file as golang.org/x/net
# suppressions; the kubectl v1.36.4 / helm v4.2.4 pins ship x/net 0.56.0,
# which cleared the module finding and left only this stdlib copy
# (verified 2026-08-21 with trivy rootfs against both release binaries).
#
# Not introduced by any change of ours: the Security workflow was green on
# 29f8aeca (2026-08-15 06:53Z) and red on the next commit three hours later,
# with only chart/manifest/test/doc changes in between — none of which reach
# these images' contents. The Go advisories landed in the Trivy DB in that
# window, so they appeared on unchanged binaries.
#
# Flagged in the scanned stable-tool images:
#   dev (Dockerfile.dev)                    docker, docker-buildx, kubectl
#   helm-installer                          helm, kubectl
# A fresh 2026-09-01 pull of the AWS Lambda Python 3.14 base scans clean for
# aws-lambda-rie, so the remaining occurrences are all in pins we track.
#
# Not clearable by an allowed pin bump — upstream re-checked 2026-09-01:
#   kubectl        v1.36.4 is latest stable-1.36 and reports go1.26.5. Both
#                  shipped kubectl pins match and CI enforces the supported
#                  ±1 minor skew against cdk.json::kubernetes_version.
#   helm           v4.2.4 is the latest stable release and reports go1.26.5;
#                  only a prerelease currently carries patched Go.
#   docker         29.7.2 is the newest static stable build.
#   docker-buildx  v0.36.1 is the latest stable release; only a prerelease
#                  currently carries patched Go.
#
# Low runtime risk. Every flagged binary is a short-lived CLI/tooling client,
# never a long-lived network service:
#   * net/http unencrypted HTTP/2 and html/template XSS are server-side
#     surfaces these clients do not expose at all.
#   * the encoding/xml, net/url and crypto/tls denial-of-service paths need
#     hostile input; these binaries talk only to trusted peers — the EKS API
#     endpoint and the version-pinned chart registries in charts.yaml, reached
#     over VPC endpoints — and encoding/asn1 is certificate parsing against
#     those same endpoints.
#   * the dev image is contributor/CI tooling and is never deployed.
#
# Clears per binary as stable upstreams ship rebuilds on Go >= 1.26.6: update
# the matching authenticated pin/checksum, keep both kubectl copies identical
# and within the CI-enforced ±1 minor skew, then remove IDs only after
# no-ignore scans show no remaining occurrence.
# Advisories: https://avd.aquasec.com/nvd/cve-2026-33818 (and the matching
#             nvd/cve-2026-XXXXX page for each id below)
CVE-2026-33818 exp:2026-11-14
CVE-2026-56853 exp:2026-11-14
CVE-2026-56858 exp:2026-11-14
CVE-2026-56859 exp:2026-11-14
CVE-2026-56860 exp:2026-11-14
CVE-2026-56862 exp:2026-11-14
CVE-2026-39821 exp:2026-11-14
CVE-2026-46600 exp:2026-11-14
# -----------------------------------------------------------------------------
# CVE-2026-56864 (a malicious GOSUMDB could serve arbitrary module content) and
# CVE-2026-56865 (a malicious GOPROXY could forge sumdb responses) —
# golang.org/x/mod at v0.38.0, statically linked into the docker-buildx CLI
# plugin that Dockerfile.dev installs (ARG BUILDX_VERSION). Surfaced by
# security:trivy:container-scan on the GCO dev image, which is the only
# scanned image carrying the Buildx plugin; every other binary in that image
# scanned clean. Fixed in x/mod 0.40.0.
#
# Not introduced by any change of ours: the Security workflow was green on
# main at 2a05133 (2026-08-19 12:14Z) and red on a pull request at
# 2026-08-19 17:45Z whose diff touches no image contents. The two x/mod
# advisories landed in the Trivy DB in that window, so they appeared on an
# unchanged buildx binary — main goes red on its next scan for the same reason.
#
# Not clearable by a stable pin bump — upstream re-checked 2026-09-01:
# Buildx v0.36.1 requires the flagged x/mod v0.38.0. v0.37.0-rc1 carries
# v0.40.0, but this release deliberately excludes prerelease tooling; remove
# these entries when a stable Buildx release carries the fix.
#
# Low runtime risk: both flaws sit in the Go module download and verification
# path — they need a hostile GOSUMDB or GOPROXY serving forged module content
# to a client that is fetching Go modules. buildx fetches no Go modules; it
# vendors x/mod for module-path and semver parsing, so the vulnerable code
# path is not reachable in this usage. The dev image is contributor/CI
# tooling and is never deployed.
#
# Clears when a buildx release ships requiring x/mod >= 0.40.0: bump
# ARG BUILDX_VERSION in Dockerfile.dev (the monthly deps-scan tracks it) and
# drop these entries.
# Advisories: https://avd.aquasec.com/nvd/cve-2026-56864
#             https://avd.aquasec.com/nvd/cve-2026-56865
CVE-2026-56864 exp:2026-11-14
CVE-2026-56865 exp:2026-11-14

# CVE-2026-84304 — google.golang.org/grpc heap memory exhaustion. Prior to
# 1.83.1, internal/transport/transport.go stores every fragmented HTTP/2 DATA
# frame as a separate recvMsg in recvBuffer, so millions of one-byte frames
# consume disproportionate heap even while staying inside the connection and
# stream flow-control windows. An unauthenticated remote peer can drive a
# gRPC *server* to a panic or OOM through concurrent multiplexed streams.
# Statically linked into the docker-buildx CLI plugin Dockerfile.dev installs
# (ARG BUILDX_VERSION), so only security:trivy:container-scan on the dev image
# surfaces it — the same binary already carrying CVE-2026-56854 and the x/mod
# pair below.
#
# Not introduced by any change of ours: the entry reached the Trivy DB between
# the green pull-request run of #322 and the push run of the identical commit
# on main, so it appeared on a dev image whose Buildx pin did not move.
#
# Not clearable by a stable pin bump — upstream checked 2026-09-02: the latest
# stable Buildx v0.36.1 requires grpc v1.82.1. v0.37.0-rc2 already carries
# v1.83.2, but this release deliberately consumes stable tooling only, and an
# availability-only defect in contributor tooling does not justify shipping a
# release candidate as every contributor's and CI job's build toolchain.
#
# Low runtime risk, and availability-only: CVSS 4.0 records VC:N/VI:N/VA:H
# with no confidentiality or integrity impact. Reaching it requires being a
# gRPC server that accepts connections from a hostile peer. buildx is a
# client: it dials a BuildKit instance the developer already controls over a
# local socket, and exposes no gRPC listener to untrusted input. The dev image
# is contributor/CI tooling and is never deployed.
#
# Clears when a stable Buildx release requires grpc >= 1.83.1: bump
# ARG BUILDX_VERSION in Dockerfile.dev with its reviewed per-architecture
# checksums (the monthly deps-scan tracks the pin) and drop this entry. Dated
# to 2026-10-01 to land in the same review as CVE-2026-56854, the other
# suppression waiting on exactly that stable Buildx release.
# Advisory: https://avd.aquasec.com/nvd/cve-2026-84304
CVE-2026-84304 exp:2026-10-01
