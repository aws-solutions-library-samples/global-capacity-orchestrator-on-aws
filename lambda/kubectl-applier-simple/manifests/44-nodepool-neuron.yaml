# Neuron NodePool — AWS Trainium (trn1, trn1n, trn2) and Inferentia (inf1, inf2)
# Purpose-built ML accelerators using the Neuron SDK instead of CUDA.
# Pods request Neuron devices via: resources.limits["aws.amazon.com/neuron"]
# Requires: Neuron device plugin (helm.aws_neuron_device_plugin.enabled = true in cdk.json)
#
# EFA support (per AWS EC2 docs, EFA-supported instance types):
#   - trn1.32xlarge / trn1n.32xlarge (Nitro v4): EFA, RDMA read+write
#   - trn2.3xlarge / trn2.48xlarge / trn2u.48xlarge (Nitro v5): EFA, RDMA read+write
#   - inf2.*: no EFA (single-node inference, no inter-node traffic)
# This pool carries ONLY the aws.amazon.com/neuron taint. It must not also
# taint vpc.amazonaws.com/efa: pods tolerating just the neuron taint (the
# shipped inferentia/trainium examples) would be unschedulable here, and
# Karpenter would never provision for them — observed live in run
# ex241-4bf01801, where both Neuron examples sat Pending to timeout. Pods
# that need EFA-attached Trainium capacity should target the dedicated EFA
# pool (43-nodepool-efa.yaml), which keeps the EFA taint.
apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: neuron-pool
spec:
  template:
    metadata:
      labels:
        workload-type: neuron
        arch: x86_64
        project: gco
        accelerator: neuron
    spec:
      nodeClassRef:
        group: eks.amazonaws.com
        kind: NodeClass
        name: default
      requirements:
        - key: "eks.amazonaws.com/instance-family"
          operator: In
          values: ["trn1", "trn1n", "trn2", "inf1", "inf2"]
        - key: "kubernetes.io/arch"
          operator: In
          values: ["amd64"]
        - key: "karpenter.sh/capacity-type"
          operator: In
          values: ["on-demand", "spot"]
      taints:
        - key: aws.amazon.com/neuron
          value: "true"
          effect: NoSchedule
  limits:
    cpu: "2000"
    memory: 8000Gi
  disruption:
    consolidationPolicy: WhenEmpty
    consolidateAfter: 300s
    budgets:
      - nodes: "10%"
